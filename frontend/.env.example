# API Configuration
# Base URL for REST API calls (defaults to "/api" when unset)
REACT_APP_API_BASE_URL=/api
# Socket.IO endpoint (defaults to same origin when unset)
REACT_APP_WS_URL=

# Environment
NODE_ENV=development

# Development Server
PORT=3000
